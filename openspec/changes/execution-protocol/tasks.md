## 1. 数据模型与判题配置

- [x] 1.1 增加 JudgeConfig、原始 stdin 输入、入口和不支持原因的模型字段与兼容默认值
- [x] 1.2 增加精确、递归浮点容差、无序多重集合比较和 stdout 规则测试

## 2. 执行器与策略协议

- [x] 2.1 实现函数结果独立通道、stdin/stdout host/Docker 执行和 JSON/text 解析
- [x] 2.2 实现简单 LeetCode 类方法适配、不支持状态和协议化提示词

## 3. 文档与验证

- [x] 3.1 更新 README 和 OpenSpec，说明输入序列化、输出空白和兼容边界
- [x] 3.2 运行真实题库回归、全量测试、静态检查、OpenSpec 校验并完成独立代码审查
