# 设计：能力雷达与覆盖热力图

`build_capability_map` 消费已有 `comparison.json`：算法设计使用正式通过率（缺失时回退样例通过率），代码实现使用样例通过率，调试使用修复率，优化使用正式通过率与 Token 消耗的效率分数，边界处理使用隐藏测试通过率。缺失数据不填 0。

热力图从每个模型/策略组合的 `by_difficulty` 和 `by_tags` 汇总，保留成功数、总数和通过率。`experiment_panel.py` 将能力数据嵌入现有 Chart.js 页面，新增能力雷达图和可读表格；离线时表格仍可查看。
