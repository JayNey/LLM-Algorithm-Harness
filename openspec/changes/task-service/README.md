# Persistent evaluation task service

Issue #14 extracts a local task lifecycle shared by the CLI and future Web/GUI clients. The change uses the incremental OpenSpec/Tweak workflow and does not add `.comet`, `.claude`, or other local workflow state.
