## 1. 策略类实现

- [x] 1.1 创建 `SelfConsistencyStrategy` 类继承 `StrategyBase`，添加 `num_candidates` 配置参数（默认 5），验证类可以实例化且参数正确传递
- [x] 1.2 实现 `execute()` 方法框架，循环生成 N 个候选解，验证方法可以被调用且返回 `ExecutionResult`
- [x] 1.3 实现候选解生成逻辑，使用 `temperature=0.8` 调用 `self.generate()` N 次，验证生成的 prompt 包含正确的温度参数
- [x] 1.4 实现候选解测试逻辑，对每个生成的候选解调用 `self.extract_code()` 和沙箱测试，验证测试结果正确记录在 `IterationResult` 中

## 2. 投票机制

- [x] 2.1 实现投票统计函数，对通过测试的候选解按代码字符串分组计数，验证相同代码能正确聚合
- [x] 2.2 实现最佳答案选择逻辑，选择频率最高的代码作为最终结果，验证在多数相同和全不同的场景下都能正确选择
- [x] 2.3 处理所有候选解失败的情况，返回失败状态，验证 `ExecutionResult.status` 为 FAILED

## 3. 结果记录

- [x] 3.1 将每个候选解记录为独立的 `IterationResult` 添加到 `ExecutionResult.iterations`，验证所有候选解都被记录
- [x] 3.2 在 `ExecutionResult` 中添加投票统计元数据（可选字段，如 `metadata` 字典），验证投票信息可序列化
- [x] 3.3 确保最终选中的代码保存在 `ExecutionResult.final_code`，验证字段值与投票结果一致

## 4. 集成和测试

- [x] 4.1 在策略工厂中注册 `SelfConsistencyStrategy`，验证可以通过配置实例化该策略
- [x] 4.2 添加单元测试覆盖投票逻辑（多数相同、全不同、全失败场景），验证 `pytest` 通过
- [x] 4.3 运行端到端测试（选择一个简单算法题），验证策略能完整执行并生成报告
- [x] 4.4 验证生成的评测报告包含所有候选解和投票统计信息

## 5. 代码审查修复 (2026-09-22)

- [x] 5.1 修复 README.md 中的重复章节（行 640-654）
- [x] 5.2 添加 num_candidates 参数验证，无效值时回退到默认值 5
- [x] 5.3 改进投票逻辑文档，说明全部候选失败时的降级行为
- [x] 5.4 添加失败候选分析，提供分类统计信息
