## 1. 基准题目集管理

- [x] 1.1 创建 benchmark 配置模块，定义 BenchmarkSuite 数据结构（name, problems, frozen, version），验证可加载示例配置文件
- [x] 1.2 实现 benchmark.json 配置文件加载和验证逻辑，验证能正确解析和报错无效配置
- [x] 1.3 添加基准题目集列表管理功能，验证能列出所有可用的 benchmark suites

## 2. CLI 命令实现

- [x] 2.1 在 CLI 中添加 `benchmark` 子命令框架，验证 `harness benchmark --help` 显示正确用法
- [x] 2.2 实现 `--suite` 参数支持，验证能指定并加载指定的 benchmark suite
- [x] 2.3 实现 benchmark 评估执行逻辑，复用现有评估框架，验证能对指定题目集完整评估

## 3. 历史数据存储

- [x] 3.1 创建 results/benchmark/ 目录结构，验证目录自动创建
- [x] 3.2 实现评估结果序列化，包含 timestamp、model_id、problems、accuracy 等字段，验证生成的 JSON 文件格式正确
- [x] 3.3 实现历史数据加载和查询功能，验证能按时间范围和模型 ID 过滤结果

## 4. 趋势分析和可视化

- [x] 4.1 实现时间序列数据提取，从历史结果中提取时间和准确率数据，验证数据结构正确
- [x] 4.2 使用 matplotlib 生成时间序列折线图（时间 × 准确率），验证生成的图表显示正确
- [x] 4.3 实现多模型对比功能，在同一图表中绘制多条趋势线，验证 `--compare` 参数生效
- [x] 4.4 添加版本对比分析，计算性能增长率和版本间差异，验证统计计算正确

## 5. 报告生成

- [x] 5.1 实现报告生成框架，定义报告结构（趋势图、统计表、里程碑），验证生成完整报告文件
- [x] 5.2 集成趋势图到报告中，验证图表正确嵌入报告
- [x] 5.3 添加统计分析部分（性能增长率、标准差、版本对比表），验证统计数据准确
- [x] 5.4 实现 `--output` 参数支持自定义报告输出路径，验证能输出到指定位置

## 6. 测试和文档

- [x] 6.1 添加单元测试覆盖核心功能（配置加载、数据存储、趋势分析），验证测试通过
- [x] 6.2 添加端到端测试，完整运行 benchmark 评估和报告生成流程，验证整个工作流正常
- [x] 6.3 编写用户文档，说明如何定义 benchmark suite、运行评估、生成报告，验证文档清晰完整
- [x] 6.4 更新 CLI 帮助文档和 README，验证 --help 输出和文档一致
