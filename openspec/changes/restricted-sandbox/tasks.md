## 1. 配置与结果模型

- [x] 1.1 先增加后端默认、资源限制和结构化失败状态测试，再扩展 SandboxConfig/SandboxResult

## 2. Docker 执行后端

- [x] 2.1 先增加 Docker 命令安全参数、环境清理和后端不可用测试，再实现 Docker runner
- [x] 2.2 先增加超时、输出上限、内存/进程限制和清理测试，再实现有界进程控制与错误映射
- [x] 2.3 先增加普通算法成功、宿主文件/网络/密钥不可见测试，再接通 SandboxExecutor 后端选择

## 3. 兼容性与交付

- [x] 3.1 保留显式 host 测试后端，更新 README、配置示例和平台限制说明
- [x] 3.2 运行针对性测试、全量测试、静态检查、OpenSpec 严格校验，并完成独立代码审查
