## 1. 配置加载与入口

- [x] 1.1 先增加 JSON/YAML、空配置和非映射配置的加载测试，再统一 `src.main` 使用配置工具；运行相关测试验证预期失败后通过
- [x] 1.2 先增加安装入口和参数优先级测试，再修正打包入口并实现显式 CLI 参数覆盖；验证 `harness --help`、模块帮助及配置文件独立运行

## 2. 参数校验与题目筛选

- [x] 2.1 先增加非正 limit、无效/空策略及难度、标签覆盖测试，再实现评测前配置校验；运行 CLI 单元测试验证
- [x] 2.2 先增加筛选结果为空的 Harness 测试，再让空结果以明确错误终止；运行 Harness 相关测试验证

## 3. 文档与完整验证

- [x] 3.1 更新 README 的安装、JSON/YAML 配置、参数优先级和筛选示例，并核对所有示例参数能被 CLI 帮助识别
- [x] 3.2 运行 OpenSpec 严格校验、针对性测试、全量测试和安装入口冒烟测试，并对照 Issue #3 的验收标准审查结果
