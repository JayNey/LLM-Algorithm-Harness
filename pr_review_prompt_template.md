# PR 审查：针对 Issue #{ISSUE_NUMBER} - {ISSUE_TITLE}

## 审查范围与目标

**仅审查与 Issue #{ISSUE_NUMBER} 相关的变更**

Issue 描述：{ISSUE_DESCRIPTION}

本次审查将检查：
1. 代码是否完全解决了 issue 中定义的问题
2. 实现是否符合项目规范和验收标准
3. 是否存在阻塞性问题（blocker）、应修复问题（shouldfix）或建议改进（nit）
4. 是否违反了本仓库的编码规范和架构约束

---

## 审查清单

### 1. Issue 完成度验证

#### Blocker（必须修复，否则不可合并）
- [ ] **验收标准覆盖**：检查 issue 中的所有验收标准是否都已实现
  - 对照 issue 中的 "验收标准" 部分，逐条检查实现
  - 缺失的验收标准将阻塞合并

- [ ] **核心功能完整性**：issue 描述的主要功能是否完全实现
  - 功能不完整或部分实现将阻塞合并

- [ ] **破坏性变更检查**：变更是否破坏现有功能或 API
  - 向后不兼容的变更需要明确说明和迁移路径

#### Should Fix（应该修复，影响代码质量）
- [ ] **测试覆盖**：新增代码是否有足够的单元测试和集成测试
  - 关键路径和边界条件是否覆盖
  - 失败场景是否有测试保护

- [ ] **错误处理**：异常和错误场景是否妥善处理
  - LLM API 调用失败
  - 沙箱执行错误
  - 边界条件和无效输入

- [ ] **文档完整性**：是否更新了相关文档
  - README.md 是否添加策略说明
  - 配置示例是否完整
  - 代码注释和 docstring 是否清晰

#### Nit（建议改进，不阻塞合并）
- [ ] **代码风格一致性**：是否遵循项目现有代码风格
- [ ] **命名规范**：变量、函数、类命名是否清晰且符合项目约定
- [ ] **代码重复**：是否有可以提取为公共方法的重复代码

---

### 2. 本仓库规范检查

#### Blocker（违反核心规范）
- [ ] **Pydantic 模型使用**：数据模型是否正确使用 Pydantic BaseModel
  - 字段类型是否有完整的类型注解
  - 是否使用 `Field(...)` 添加描述和验证
  - 是否使用 `model_validator` 或 `field_validator` 进行复杂验证

- [ ] **策略基类继承**：新策略是否正确继承 `StrategyBase`
  - 是否实现了必需的 `execute()` 方法
  - 是否使用 `self.generate()` 而非直接调用 `llm_client.generate()`
  - 是否复用基类的工具方法（`extract_code`, `create_iteration_result` 等）

- [ ] **沙箱执行规范**：是否正确使用 SandboxExecutor
  - 是否捕获沙箱执行异常并记录 `sandbox_error`
  - 是否正确处理 `SandboxResult`

#### Should Fix（违反最佳实践）
- [ ] **结构化日志**：是否使用项目的结构化日志系统
  - 使用 `get_logger(__name__)` 而非标准 `logging`
  - 日志事件名使用 snake_case（如 `"candidate_generated"`）
  - 日志使用结构化字段而非字符串拼接
  
  ```python
  # ✓ 正确
  self.logger.info("strategy_initialized", num_candidates=5)
  
  # ✗ 错误
  self.logger.info(f"Strategy initialized with {5} candidates")
  ```

- [ ] **密钥脱敏**：涉及敏感信息的日志是否脱敏
  - 使用 `redact_sensitive_data()` 或 `redact_sensitive_text()`
  - 配置、trace、报告中不暴露 API 密钥

- [ ] **ExecutionResult 构建**：是否正确记录执行结果
  - 使用 `create_execution_result()` 构建结果
  - `llm_traces` 是否记录了完整的调用参数和元数据
  - Token 统计是否准确（多候选解场景）

- [ ] **策略注册**：是否在策略工厂中注册新策略
  - 检查是否更新了 `src/strategies/__init__.py`
  - 检查是否更新了 `harness.py` 的 `STRATEGY_MAP`

#### Nit（代码质量建议）
- [ ] **类型注解完整性**：函数签名是否有完整的类型注解
- [ ] **Magic Number**：是否使用常量或配置替代硬编码数字
- [ ] **注释必要性**：复杂逻辑是否有必要的注释说明

---

### 3. Issue 特定检查

> 根据具体 issue 的类型和需求，添加针对性的检查项

#### 业务逻辑检查
- [ ] **核心算法正确性**：实现的算法或策略是否符合 issue 描述的预期行为
- [ ] **边界条件处理**：是否正确处理边界情况和极端输入
- [ ] **状态管理**：如果涉及状态管理，状态转换是否正确

#### 性能与资源
- [ ] **性能影响**：变更是否引入明显的性能退化
- [ ] **资源消耗**：是否正确统计和记录资源消耗（Token、时间、内存等）
- [ ] **并发安全**：如果涉及并发，是否有线程安全或资源竞争问题

#### 集成与兼容性
- [ ] **向后兼容**：变更是否与现有配置和数据格式兼容
- [ ] **依赖管理**：是否引入新依赖，依赖版本是否合理
- [ ] **环境适配**：是否在不同环境下都能正常工作

---

## 审查输出格式

请按以下格式组织审查结果：

### ✅ 已正确实现
列出 issue 中已被正确解决且符合规范的部分

### 🚫 Blocker（必须修复）
- **[位置]** 问题描述
  - 违反的规范/缺失的验收标准
  - 建议的修复方案

### ⚠️ Should Fix（应该修复）
- **[位置]** 问题描述
  - 影响的质量维度
  - 建议的改进方案

### 💡 Nit（可选改进）
- **[位置]** 建议描述
  - 改进理由
  - 可选的实现建议

### 📊 Issue 完成度评估
- 验收标准完成度：X/Y
- 核心功能完成度：X%
- 测试覆盖完成度：X%
- 文档更新完成度：X%

### 🎯 总体评价
- [ ] **建议合并**：所有 blocker 已解决，should fix 可后续处理
- [ ] **需要修改**：存在 blocker 问题，必须修复后才能合并
- [ ] **需要重大修改**：核心设计或实现存在严重问题

---

## 审查原则

1. **聚焦 Issue**：只审查与 issue 直接相关的变更，不扩大审查范围
2. **验收驱动**：以 issue 中的验收标准为准，而非个人偏好
3. **规范优先**：项目规范和架构约束高于个人编码习惯
4. **分级明确**：严格区分 blocker、should fix、nit，不混淆优先级
5. **建设性反馈**：指出问题的同时给出具体改进建议
6. **上下文感知**：考虑项目现有代码的风格和模式，保持一致性

---

## 使用说明

1. **填写 Issue 信息**：将 `{ISSUE_NUMBER}`、`{ISSUE_TITLE}`、`{ISSUE_DESCRIPTION}` 替换为实际的 issue 内容

2. **自定义 Issue 特定检查**：
   - 阅读 issue 的具体需求和技术细节
   - 在"Issue 特定检查"章节添加针对该 issue 的业务逻辑检查项
   - 例如：新增策略需要检查算法正确性，新增导入器需要检查数据转换准确性

3. **执行审查**：
   - 逐项检查清单，标记发现的问题
   - 记录问题的具体位置（文件名:行号）
   - 区分 blocker、should fix、nit 三个级别

4. **输出结果**：
   - 按照"审查输出格式"整理发现
   - 提供具体的修复建议，而非泛泛而谈
   - 给出明确的合并建议

5. **保持客观**：
   - 以 issue 验收标准和项目规范为准
   - 避免基于个人偏好的主观评价
   - 对积极方面也给予认可

---

## 审查示例

### Issue 信息
- **Issue Number**: #16
- **Issue Title**: [功能] 新增 Self-Consistency 求解策略
- **Issue Description**: 实现基于多样本投票的 Self-Consistency 策略，通过并行生成多个候选解并投票选择最佳答案

### 审查发现

#### ✅ 已正确实现
- Self-Consistency 核心投票机制实现正确
- 结构化日志使用规范，事件名和字段符合项目约定
- Pydantic 模型使用正确，类型注解完整
- 错误处理覆盖 LLM API 失败和沙箱执行失败场景
- README 文档已更新，包含策略说明和示例输出

#### 🚫 Blocker
- **[src/strategies/self_consistency.py:89]** 温度参数硬编码为 0.8
  - 违反规范：策略参数应通过配置传递，支持用户自定义
  - 验收标准：需要支持温度参数配置以增加候选解多样性
  - 建议：在 `StrategyConfig` 中使用 `temperature` 字段，通过 `self.config.temperature` 获取

- **[README.md:242-249]** 配置示例不完整
  - 验收标准：文档应包含完整的配置示例
  - 问题：缺少 `temperature` 参数说明
  - 建议：补充温度参数的配置说明和推荐值

#### ⚠️ Should Fix
- **[src/strategies/self_consistency.py:241]** 全失败场景返回最后一个候选的结果
  - 影响：可能丢失其他候选的有价值错误信息
  - 建议：考虑聚合所有失败信息，或返回最常见的失败类型

- **[tests/strategies/]** 缺少 `test_self_consistency.py` 单元测试文件
  - 验收标准：添加单元测试覆盖投票逻辑
  - 建议：添加测试用例覆盖：多数相同、全不同、全失败三种场景

- **[src/strategies/__init__.py]** 未确认是否注册了新策略
  - 影响：用户可能无法通过配置文件使用该策略
  - 建议：确认 `__init__.py` 和 `harness.py` 是否已注册 `SelfConsistencyStrategy`

#### 💡 Nit
- **[src/strategies/self_consistency.py:35]** `num_candidates` 默认值可以提取为类常量
  - 改进理由：提高可读性和可维护性
  - 建议：`DEFAULT_NUM_CANDIDATES = 5`

- **[src/strategies/self_consistency.py:248]** 投票统计可以添加代码哈希避免完整代码存储
  - 改进理由：大型代码块会导致报告体积膨胀
  - 建议：考虑存储代码哈希 + 首次出现的完整代码

#### 📊 Issue 完成度评估
- 验收标准完成度：4/6（温度配置和测试覆盖待完善）
- 核心功能完成度：95%（投票机制完整，配置灵活性待提升）
- 测试覆盖完成度：60%（端到端可能已测试，单元测试待补充）
- 文档更新完成度：85%（README 已更新，温度参数说明待补充）

#### 🎯 总体评价
- [x] **需要修改**：存在 2 个 blocker（温度硬编码、文档不完整），修复后可合并
- [ ] ~~建议合并~~
- [ ] ~~需要重大修改~~

**建议**：
1. 优先修复温度参数硬编码问题，改为通过配置传递
2. 补充 README 中的温度参数说明
3. 建议补充单元测试（可在后续 PR 中完成）
4. Should fix 和 nit 问题可在合并后逐步改进

**正面评价**：
- 代码整体质量高，日志和错误处理完善
- 投票机制实现清晰，易于理解和维护
- 失败分析功能是额外的亮点，超出了基本需求
